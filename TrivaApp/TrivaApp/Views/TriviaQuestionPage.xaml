<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="TrivaApp.Views.TriviaQuestionPage">
    <ContentPage.Content>
            <Grid Padding="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                     <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
            <Label Text="{Binding Question}" FontSize="20" HorizontalOptions="Center" Grid.Row="0" Grid.ColumnSpan="2"/>

            <Button Text="True" Command="{Binding TrueCommand}" Grid.Row="1"/>

            <Button Text="False" Command="{Binding FalseCommand}" Grid.Row="1" Grid.Column="1"/>

            <Label Text="{Binding ResultText}" FontSize="14" HorizontalOptions="Center" Grid.Row="2" Grid.ColumnSpan="2"/>

            <Button Text="Back" Command="{Binding BackCommand}" Grid.Row="4"/>

            <Button Text="Next" Command="{Binding NextCommand}" Grid.Row="4" Grid.Column="1"/>
            </Grid>          
    </ContentPage.Content>
</ContentPage>

